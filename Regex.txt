while (Placa == null)
                            if (Regex.IsMatch(Placa, @""))
                            {
                                Placa = Console.ReadLine();
                            }



      // Console.WriteLine("Qual a capacidade do tanque ? ");
            //CapTanque = byte.Parse(Console.ReadLine());

          //  if (_flex == true)
        //    {
             //   Console.WriteLine("Quantos litros de gasolina há em seu carro ? ");
           //     Gasolina = double.Parse(Console.ReadLine());
           //     Console.WriteLine("Quantos litros de alcool há em seu carro ?");
            //    Alcool = double.Parse(Console.ReadLine());
             //   VerifyQuantity();
          //  }
          //  else
        //    {
           //     Console.WriteLine("Quantos litros de gasolina há em seu carro ? ");
         //       Gasolina = double.Parse(Console.ReadLine());
         //   }